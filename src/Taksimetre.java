public class Taksimetre {

    /*
        Scanner input = new Scanner(System.in);
        int km;
        double kmPrice = 2.20;
        double total;
        int startPrice = 10;
        System.out.println("katedilen km:");
        km = input.nextInt();
       total = km*kmPrice;
       total += 10;

       total = (total>20) ? total : 20;
       System.out.println("toplam fiyat:" +total);
     */
}
