public class DiziTekrari {

    /*
     int[] dizi = {10, 20, 20, 10, 10, 20, 5, 20};

        // Diziyi sıralayarak benzersiz elemanları bulma
        Arrays.sort(dizi);

        for (int i = 0; i < dizi.length; i++) {
            int eleman = dizi[i];
            int tekrarSayisi = 1;

            // Aynı elemanın ardışık tekrarlarını say
            while (i < dizi.length - 1 && dizi[i] == dizi[i + 1]) {
                tekrarSayisi++;
                i++;
            }

            System.out.println(eleman + " sayısı " + tekrarSayisi + " kere tekrar edildi.");
        }
    }
     */
}
