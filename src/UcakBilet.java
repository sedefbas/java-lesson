public class UcakBilet {
    /*
    {
        Scanner input = new Scanner(System.in);
        int km;
        int yas;
        int yolculukTipi;
        double kmPrice = 0.10;
        double totalPrice ;
        System.out.print("mesafe km cinsinden: ");
         km = input.nextInt();
        System.out.print("yasiniz: ");
        yas = input.nextInt();
        System.out.println("1:tek yön\n2:gidiş-donuş");
        System.out.print("yolculuk tipi seçiniz");
        yolculukTipi = input.nextInt();
        totalPrice = (yolculukTipi == 2) ? 2*km*kmPrice :  km*kmPrice;
        System.out.println("ilk fiyat:" + totalPrice);

        if(yas>0 && km>0 && (yolculukTipi == 1 || yolculukTipi ==2 )){
            System.out.println("giriş başarılı");
            if(yas<12)
            {   totalPrice = totalPrice/2;
                System.out.println("indirimli fiyat: " + totalPrice);
                if(yolculukTipi==2){
                    totalPrice=totalPrice-totalPrice*0.2;
                    System.out.println("son fiyat:" + totalPrice);
                }
            } else if (yas>12 && yas<24) {
                totalPrice = totalPrice - totalPrice*0.1;
                System.out.println("indirimli fiyat " + totalPrice);
                if(yolculukTipi==2){
                    totalPrice=totalPrice-totalPrice*0.2;
                    System.out.println("son fiyat:" + totalPrice);
                }
            } else if (yas>35) {
                totalPrice = totalPrice - totalPrice*0.3;
                System.out.println("indirimli fiyat " + totalPrice);
                if(yolculukTipi==2){
                    totalPrice=totalPrice-totalPrice*0.2;
                    System.out.println("son fiyat:" + totalPrice);
                }
            }
        }
        else {
            System.out.println("hatalı veri girdiniz");
        }
    }
     */
}
